import {createStyles} from "@material-ui/core/styles";

export const errorPageStyle = createStyles({
  content: {
    flexGrow: 24,
    width: "100%"
  },
  root: {
    height: "100%",
    width: "100%"
  }
});
